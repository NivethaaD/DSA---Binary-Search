class Solution:
    def rowAndMaximumOnes(self, mat):
        maxi = 0
        ind = 0

        for i in range(len(mat)):
            count = sum(mat[i])
            if count > maxi:
                maxi = count
                ind = i

        return [ind, maxi]
